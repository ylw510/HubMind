# 复制为 .env 并填写实际值
# cp .env.example .env

# PostgreSQL（必填）
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# 需先创建数据库，例如: createdb hubmind  或  CREATE DATABASE hubmind;
DATABASE_URL=postgresql://hubmind:your_password@localhost:5432/hubmind

# 可选：JWT 密钥（生产环境务必修改）
# JWT_SECRET_KEY=your-random-secret-key

# 以下为 LLM / GitHub 等，可在「设置」中按用户配置，此处为未登录时的默认值
# GITHUB_TOKEN=
# LLM_PROVIDER=deepseek
# DEEPSEEK_API_KEY=
